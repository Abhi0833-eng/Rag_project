# =========================================
# RAG (Retrieval-Augmented Generation) Project
# LLM Provider: Mistral AI
# =========================================

# ---------- Core LLM / Orchestration ----------
langchain==0.3.7
langchain-community==0.3.7
langchain-core>=0.3.17,<0.4.0
langchain-mistralai==0.2.2      # Mistral AI integration for LangChain (chat + embeddings)
mistralai==1.2.3                # Official Mistral AI Python SDK

# ---------- Embeddings ----------
sentence-transformers>=3.2.1    # Local/open-source embedding models (alternative to Mistral embeddings)

# ---------- Vector Databases (pick what you use) ----------
faiss-cpu>=1.9.0                # Lightweight local vector store
chromadb>=0.5.20                # Popular local/persistent vector DB
pinecone-client>=5.0.1          # Managed cloud vector DB (optional)
qdrant-client>=1.12.1           # Alternative vector DB (optional)

# ---------- Document Loading & Parsing ----------
pypdf>=5.1.0                    # PDF parsing
python-docx>=1.1.2              # Word document parsing
unstructured>=0.16.6            # Multi-format document loader (PDF, HTML, DOCX, etc.)
beautifulsoup4>=4.12.3          # HTML/web scraping parsing
tiktoken>=0.8.0                 # Tokenization for chunk sizing

# ---------- Text Splitting / Chunking ----------
langchain-text-splitters==0.3.2

# ---------- Environment & Config ----------
python-dotenv>=1.0.1            # Load API keys from .env file
pydantic>=2.9.2                 # Data validation (used internally by LangChain too)

# ---------- API / Backend (optional, if serving RAG as an app) ----------
fastapi>=0.115.5
uvicorn>=0.32.0

# ---------- Frontend / UI (optional) ----------
streamlit>=1.40.1               # Quick UI for RAG demo/chat interface

# ---------- Utilities ----------
numpy>=1.26.4
pandas>=2.2.3
tqdm>=4.67.0                    # Progress bars for indexing large corpora
requests>=2.32.3

# ---------- Logging / Tracing (optional but recommended) ----------
langsmith>=0.1.147              # Trace and debug LangChain/LangGraph pipelines
